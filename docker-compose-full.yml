version: '3'

services:

    nginx:
        container_name: coreapi_nginx
        image: nginx:1.11-alpine
        ports:
            - "8181:80"
        volumes:
            - ./docker/nginx/nginx.conf:/etc/nginx/conf.d/default.conf
            - .:/app
        depends_on:
            - php

    php:
        container_name: coreapi_php
        image: jorge07/alpine-php:7.2-dev-sf
        ports:
            - "9000:9000"
        volumes:
            - .:/app
        depends_on:
            - mysql

    mysql:
        container_name: coreapi_mysql
        ports:
            - 3306:3306
        restart: always
        environment:
            MYSQL_ROOT_PASSWORD: root
            MYSQL_DATABASE: core_api_dev
            MYSQL_USER: amiab
            MYSQL_PASSWORD: iPj!myVCkW6
        volumes:
            - mysql_dev:/var/lib/mysql
            - .:/app
        image: mysql:5.7


volumes:
    mysql_dev:
